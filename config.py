# This is the configuration file. Use it to store variables that will appear
# across multiple files

# TECHNICALLY, you can set a remote host, but will confuse the server if you
# run it locally.
HOST = "localhost"

# This should probably be left alone
PORT = 9999

# Work and play settings apparently defined by the Pomodoro Method, but I don't
# see why I can't make them configurable
WORK_TIME = 60 * 1 # Use this to denote the actual seconds
PLAY_TIME = 60 * 1

# File Location This will be EXTREMELY important when this becomes a service
FILELOC = 'first.dat'
